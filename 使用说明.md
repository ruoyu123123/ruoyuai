# 若渝AI · 使用说明

一个陪你写小说的 AI 助手。你负责想故事，剩下的它来。

---

## 这是什么

若渝AI 帮你：

- **蒸馏作者风格** — 把你喜欢的作者的写作 DNA 提取出来，照着那种感觉写
- **生成大纲** — 规划卷级大势、决定每卷故事块数、埋伏笔
- **故事块写作（v26 起）** — writer 一气呵成写整块 → 质检 → 最后才切章；每写完一块自动保存状态、涌现下一块走向
- **v27 自由发挥** — writer 不预知章数/字数 · splitter 按 3000-4500/章字数硬范围自然切
- **质量保障** — 自动检测 AI 腔调、角色前后矛盾、字数偏离
- **版本回溯** — 每本小说自动 Git 存档，想回到哪章都行

---

## 快速开始（3 步上手）

### 第 1 步：打招呼

直接对助手说「你好」或「写小说」，会看到菜单：

```
[1] 新建项目（选择/蒸馏风格）
[2] 继续写作（恢复上次进度）
[3] 自由模式（不用参考风格）
...
```

### 第 2 步：选一个方式开始

**A · 有喜欢的作者？蒸馏他的风格**

```
我想模仿《斗破苍穹》的风格
```

或直接发链接：

```
/distill-style https://www.biquge.com/book/123/
```

也可以发本地文件路径：

```
/distill-style D:\小说\斗破苍穹.txt
```

AI 会逐章蒸馏（每章 1 个子任务），全书蒸馏完成后存到风格库，后续随时调用。

**B · 不讲究风格，直接写**

```
帮我写个都市爽文，男主是外卖员
```

AI 会生成 3 个灵感，挑一个（或让它换一批）。

### 第 3 步：确认后开写

AI 会：

1. 生成卷级大纲（只详化首个故事块，后续按涟漪涌现）
2. 询问每卷想要几个故事块（cluster · 推荐 6-8）+ 初始化 34 个核心子系统 JSON + Git 仓库
3. 直接开始写第一个故事块（一般切出 3-7 章）
4. 每写完一块展示 2-3 张「剧情走向卡片」，你选哪张就往哪走

```
📦 第 1 个故事块写好了（5 章 · 18420 字）
📌 已快照：a1b2c3d

接下来怎么走？

[A] 正面对抗 — 主角当场揭穿反派
[B] 暗中周旋 — 主角假装不知道，等待时机
[C] 撤退保命 — 先跑，稳住再说

输入编号，或直接说你的想法。
```

---

## 常用场景

### 想继续上次没写完的

```
/continue
```

AI 自动检测中断点（故事块写了一半？cluster-save-state 没跑完？pending_tail 待补料？）然后从正确的地方接着走。

### 想看哪些风格可用

```
/db list-styles
```

或直接查看 `workspace/styles/` 目录下的 `.json` 文件。

### 写到一半发现设定错了

比如主角瞳色想从黑改金：

```
/reconcile 主角的瞳色从"黑色"改为"金色"
```

AI 会扫描所有已写章节，找出受影响的段落，给你 4 个选项（全部自动改 / 只改重要的 / 逐章确认 / 只改档案不改正文）。调和前自动打 Git 标签，不满意一键回滚。

### 想对主角做一次深度挖掘

```
/distill-character 李若渝
```

从已写章节里提取角色的 5 层人格（说话方式、情绪模式、决策倾向、认知盲区…），写入人物卡。后续章节写这个角色会更一致。

### 觉得某章 AI 味太重

```
/anti-slop 第 5 章
```

机械扫描禁用词、句式均匀度、对话占比，给出修改建议。

### 想全面体检

```
/check-quality
```

质量 + 正典一致性 + 风格保真度三维校验。

---

## 命令速查表

### 核心流程

| 命令 | 用途 |
|------|------|
| `/write` | 完整写小说流程（选风格 → 灵感 → 大纲 → 逐故事块） |
| `/continue` | 断点续写 |
| `/outline` | 单独生成大纲 + 初始化项目（含 step 1.7 询问每卷 cluster 数） |
| `/cluster-write` | 写一个故事块（7 步流水线 · 自由发挥默认） |
| `/cluster-save-state` | 保存故事块状态 + 涌现下个故事块走向卡（12 步） |

> 🔴 **v26 起 `/write-chapter` `/save-state` 单章命令已废弃**：写作单位是故事块（cluster），整块迭代→最后才切章。

### 蒸馏系统

| 命令 | 用途 |
|------|------|
| `/distill-style` | 从参考小说蒸馏作者风格 |
| `/distill-character` | 深度蒸馏已出场角色 |

### 质量保障

| 命令 | 用途 |
|------|------|
| `/check-quality` | 质量 + 正典 + 风格三维校验 |
| `/anti-slop` | 机械扫描 AI 腔调 |
| `/reconcile` | 设定修改后一致性调和 |
| `/foreshadowing` | 契诃夫之枪检测 |

### 工具

| 命令 | 用途 |
|------|------|
| `/db` | 查看/管理数据库 |
| `/session-start` | 恢复写作会话 |
| `/brainstorm` | 生成故事灵感 |

---

## 写作流程的骨架

```
你               AI
 │                │
 │  打招呼        │
 ├───────────────►│  展示菜单
 │                │
 │  选风格/灵感    │
 ├───────────────►│  /distill-style 或 /brainstorm
 │                │
 │  确认           │
 ├───────────────►│  /outline 生成大纲 + 建库 + git init
 │                │                   └──► 📌 commit: "chore: 初始化"
 │                │                   └──► 📌 commit: "feat: 生成大纲"
 │                │
 │                │  /cluster-write 写第 1 个故事块（7 步 · gen-model 出整块 18K 字）
 │                │    ↓             └──► splitter 按字数切 5 章（v27）
 │                │  /cluster-save-state（12 步流水线）
 │                │    ↓             └──► 📌 commit: "feat(cluster-001): 5 章 (ch1-5)"
 │                │  展示走向卡片 → 涌现下个故事块 brief
 │                │
 │  选卡片        │
 ├───────────────►│  写下个故事块 → cluster-save-state → 卡片
 │                │  ...
 │                │
 │  全书完成       │
 │◄───────────────┤  拼接全文.txt + 最终 commit
 │                │
```

**全程只有一个停顿点**：每章结束的小势卡片。其他都是自动的。

如果你说「全自动」，连小势卡片也会自动选「默认走向」，AI 会一口气写到结束（适合出稿）。

---

## Git 版本回溯（进阶玩法）

每本小说都是独立的 Git 仓库。关键节点自动 commit：

| 节点 | Commit 示例 |
|------|-------------|
| 初始化 | `chore: 初始化项目 + 34 个核心子系统 JSON` |
| 大纲完成 | `feat: 生成大纲（V1 7 cluster · 仅详化 cluster_001）` |
| 故事块保存 | `feat(cluster-005): 5 章 (ch21-25)` |
| 风格蒸馏 | `feat: 蒸馏作者风格 v3（2033 章）` |
| 角色蒸馏 | `feat: 蒸馏角色 李若渝 v2（ch 1-12）` |
| 一致性调和 | `fix: 调和 主角瞳色（影响 3 章）` |

### 实用命令（在项目目录里跑）

**查看所有版本：**

```bash
cd 小说_书名
git log --oneline
```

**看某章改过哪些次：**

```bash
git log --oneline -- 第5章.txt
```

**回到某一章的当时版本：**

```bash
git checkout <commit-hash> -- 第5章.txt
```

**调和不满意，整体回滚：**

```bash
# 调和前的 tag 会打在 before-reconcile-<时间戳>
git reset --hard before-reconcile-20260510_143022
```

**对比两个版本：**

```bash
git diff <旧hash> <新hash> -- 第5章.txt
```

AI 不会帮你做破坏性操作（push/force/reset --hard），这些是你自己手动决定的安全网。

---

## 项目目录长什么样

```
workspace/novels/{书名}/              # 每本小说一个独立 Git 仓库
├── .git/                            # Git 仓库（自动）
├── .gitignore                       # 排除临时文件（自动）
├── 章节/                            # v26 起所有章节产物在此
│   ├── cluster_001_draft/           # 故事块草稿 + cluster_changes.json + pending_tail.txt
│   ├── 第001章/                    # splitter 切完的章 + 第NNN章_changes.json
│   ├── 第002章/
│   └── ...
├── 全文.txt                         # 完成后自动拼接
└── _数据库/                          # 34 个核心子系统 JSON
    ├── 进度.json                    # 卷级大纲 + chapter_plan + 传播债务
    ├── 事件簇.json                  # 故事块定义（cluster brief · _writer_mode）
    ├── 大势卡.json                  # ME 池 + rhythm_profile + 节奏模式
    ├── 人物卡.json                  # 含 5 层 Voice DNA + 声音包
    ├── 世界观.json                  # 设定 + 关键词触发条目
    ├── 世界状态.json                # 涟漪传播实时状态（v25）
    ├── 涟漪规则.json                # 走向卡 → 世界变化规则
    ├── 伏笔表.json                  # 4 类约束（promises/deadlines/pledges/secrets）
    ├── 章纲摘要.json                # 每章 200 字摘要 + 情绪值
    ├── 地图.json 关系.json 时间线.json 道具.json 事件表.json 场景规则.json
    ├── 写作经验.json                # 从成功/失败中学到的经验
    ├── 用户偏好.json                # 含 v27 cluster_count_per_volume / writer_mode
    ├── 作者风格.json                # 蒸馏出的风格档案（可选）
    └── .wal/ .audit/ .reading_reflection/ .judge_reports/    # 临时日志

workspace/styles/{书名}/             # 跨项目共享的风格库
├── 作者风格_FINAL.json
├── skill_FINAL.md
├── 蒸馏进度/
└── ...
```

---

## 写作模式

告诉 AI 你想要哪种：

- **完整模式**（默认）— 所有系统激活，适合长篇
- **轻量模式** — 只用核心系统，适合短篇快速出稿

切换方式：

```
切换到轻量模式
```

---

## 常见问题

**Q：可以不蒸馏风格直接写吗？**

可以，选「自由模式」或「[3]」，AI 用默认网文风格写。

**Q：AI 写着写着角色不像自己了？**

跑 `/distill-character 角色名`，重新校准该角色的 Voice DNA。

**Q：一部书写了一半想换个主角性格？**

用 `/reconcile` 描述变更，AI 会扫描历史章节、打 Git 标签、让你选批量修改方式。

**Q：没跑完 save-state 就关电脑了？**

下次说 `/continue` 即可。WAL 日志会识别中断点，从未完成的步骤接着跑。

**Q：想把已写的几章换个作者风格重写？**

先 `/distill-style 新作者.txt`，然后 `/reconcile 换风格` 让 AI 批量改写（慎用，工作量大）。

**Q：Git 里看不到小说文件？**

确认项目目录有 `.git/`。没有的话在项目目录执行 `git init`，AI 下次保存就会自动 commit。

**Q：章节字数不够怎么办？**

save-state 的第 7.4 步会自动检测。偏离 >20% 会启动 Agent 子任务补写；偏离 10-20% 会问你要不要补。

**Q：作者风格蒸馏很慢？**

正常的。全书 2000+ 章必须逐章蒸馏（这是设计上的硬性规则，防止采样偷懒）。每 10 章会更新一次 skill 文件，你能看到进度。

---

## 不要做的事

- ❌ 别手动编辑 `_数据库/` 里的 JSON 文件（除非你知道在干嘛）——写错会影响后续注入
- ❌ 别在写作时手动 `git reset --hard` 到很早的章节——会让数据库和章节对不上
- ❌ 别删除 `.wal/` 目录里正在进行中的文件——会丢失崩溃恢复点

**想修改设定就用 `/reconcile`**，想回滚就用 Git 标签，想修档案就用 `/db`。

---

## 速记：最常用的 5 个命令

```
/write              # 新建小说，从零开始
/continue           # 继续上次
/check-quality      # 体检
/reconcile 描述     # 改设定
/distill-character  # 重新校准角色
```

掌握这 5 个就能用 90% 的功能。

---

**开始写吧。有问题随时问我。**
